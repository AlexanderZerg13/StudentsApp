<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.example.pilipenko.studentsapp"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:name=".StudentsAppApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.LoginActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.ChooseUniversityActivity"
            android:theme="@style/AppTheme.Back"
            android:configChanges="orientation"
            android:screenOrientation="portrait">
        </activity>
        <activity android:name=".ui.activity.MainContentActivity">

        </activity>
        <activity
            android:theme="@style/AppTheme.Back"
            android:label="@string/settings"
            android:parentActivityName=".ui.activity.LoginActivity"
            android:name=".ui.activity.SettingActivity"/>

        <service
            android:name=".service.LoginIntentService"
            android:exported="false"/>

        <service
            android:name=".service.FetchDataIntentService"
            android:exported="false"/>
    </application>

</manifest>
